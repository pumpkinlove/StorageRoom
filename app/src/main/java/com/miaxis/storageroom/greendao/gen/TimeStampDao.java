package com.miaxis.storageroom.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.miaxis.storageroom.bean.TimeStamp;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "TIME_STAMP".
*/
public class TimeStampDao extends AbstractDao<TimeStamp, Long> {

    public static final String TABLENAME = "TIME_STAMP";

    /**
     * Properties of entity TimeStamp.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, long.class, "id", true, "_id");
        public final static Property StampName = new Property(1, String.class, "stampName", false, "STAMP_NAME");
        public final static Property OpDateTime = new Property(2, String.class, "opDateTime", false, "OP_DATE_TIME");
    }


    public TimeStampDao(DaoConfig config) {
        super(config);
    }
    
    public TimeStampDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"TIME_STAMP\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: id
                "\"STAMP_NAME\" TEXT," + // 1: stampName
                "\"OP_DATE_TIME\" TEXT);"); // 2: opDateTime
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"TIME_STAMP\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, TimeStamp entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String stampName = entity.getStampName();
        if (stampName != null) {
            stmt.bindString(2, stampName);
        }
 
        String opDateTime = entity.getOpDateTime();
        if (opDateTime != null) {
            stmt.bindString(3, opDateTime);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, TimeStamp entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getId());
 
        String stampName = entity.getStampName();
        if (stampName != null) {
            stmt.bindString(2, stampName);
        }
 
        String opDateTime = entity.getOpDateTime();
        if (opDateTime != null) {
            stmt.bindString(3, opDateTime);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public TimeStamp readEntity(Cursor cursor, int offset) {
        TimeStamp entity = new TimeStamp( //
            cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // stampName
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // opDateTime
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, TimeStamp entity, int offset) {
        entity.setId(cursor.getLong(offset + 0));
        entity.setStampName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setOpDateTime(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(TimeStamp entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(TimeStamp entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(TimeStamp entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
